{
  "name": "package-version-info",
  "version": "0.2.4",
  "author": {
    "name": "Dominik Hladík",
    "email": "dominik.hladik@seznam.cz",
    "url": "https://github.com/Celtian"
  },
  "license": "MIT",
  "private": false,
  "description": "Generate version info from package.json for TypeScript projects",
  "scripts": {
    "validate": "bun run format:check && bun run test:launcher && bun run build && bun run test",
    "format:check": "zig fmt --check src/root.zig src/main.zig src/cli.zig build.zig",
    "build": "zig build",
    "build:platforms": "bun run script:package-zig && bun ./scripts/build-platforms.ts",
    "start": "zig build run",
    "test": "zig build test",
    "test:launcher": "node --test ./scripts/package-version-info.test.cjs",
    "test:package": "bun ./scripts/verify-package.ts",
    "coverage:build": "rimraf coverage zig-out/coverage-tests && zig build coverage",
    "clean": "rimraf zig-out dist",
    "prepare": "husky",
    "package": "bun run build:platforms && bun run script:package-npm && bun run test:package",
    "postinstall": "bun run script:package-zig",
    "script:package-zig": "bun ./scripts/package-zig.ts",
    "script:package-npm": "bun ./scripts/package-npm.ts",
    "script:package-github": "bun ./scripts/package-github.ts",
    "version": "auto-changelog -p && git add CHANGELOG.md",
    "postversion": "git push && git push --follow-tags",
    "publish:beta": "bun install && bun run validate && bun run package && npm publish --folder dist --tag beta",
    "release:beta": "npm version prerelease -m \"chore(update): prelease %s β\"",
    "release:patch": "git checkout master && npm version patch -m \"chore(update): patch release %s 🐛 \"",
    "release:minor": "git checkout master && npm version minor -m \"chore(update): release %s 🚀\"",
    "release:major": "git checkout master && npm version major -m \"chore(update): major release %s 💥 \"",
    "fix-vulnerabilities": "bun update && bun audit"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "@types/fs-extra": "^11.0.4",
    "auto-changelog": "^2.6.0",
    "copyfiles": "^2.4.1",
    "fs-extra": "^11.3.6",
    "husky": "^9.1.7",
    "rimraf": "^6.1.3"
  },
  "bin": {
    "package-version-info": "zig-out/bin/version_info"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "node": ">=24 <26"
  },
  "homepage": "https://github.com/Celtian/package-version-info",
  "repository": {
    "url": "git+https://github.com/Celtian/package-version-info.git",
    "type": "git"
  },
  "bugs": {
    "url": "https://github.com/Celtian/package-version-info/issues"
  },
  "auto-changelog": {
    "remote": "none",
    "commitUrl": "https://github.com/Celtian/package-version-info/commit/{id}",
    "issueUrl": "https://github.com/Celtian/package-version-info/issues/{id}",
    "mergeUrl": "https://github.com/Celtian/package-version-info/pull/{id}",
    "compareUrl": "https://github.com/Celtian/package-version-info/compare/{from}...{to}"
  },
  "keywords": [
    "version",
    "info",
    "typescript",
    "zig",
    "package"
  ],
  "files": [
    "dist"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "dependencies": {}
}
